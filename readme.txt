if you want play sound effect,

First, install sound libraries
on the top menue, clcik 
"Sketch" -> import library -> add library 
-> in the Libraries tap, find "Minim" and install that library.

- Done - 

Second, you need add file. 

on the top menue, clcik 
"Sketch" -> Add file -> find your sound source ( I put sound source in same folder called "click.wav")